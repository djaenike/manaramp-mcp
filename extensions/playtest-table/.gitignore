node_modules

# Generated by `wrangler types` from wrangler.jsonc — regenerated on every build (see package.json's
# build script), never meaningfully committable since it goes stale the moment wrangler.jsonc changes
# and nothing enforces re-committing it.
worker-configuration.d.ts

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# wrangler's local secret-injection file (e.g. ANTHROPIC_API_KEY for testing ai-turn.ts locally) —
# not matched by the .env* patterns above since wrangler looks for this exact filename.
.dev.vars

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*
